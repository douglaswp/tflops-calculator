services:
  hardware-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hardware-ai-dashboard
    ports:
      # Mapeia a porta 8080 do seu computador para a porta 80 do Nginx no container
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production