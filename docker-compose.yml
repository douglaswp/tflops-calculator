services:
  hardware-dashboard:
    image: ghcr.io/douglaswp/tflops-calculator:latest
    container_name: hardware-ai-dashboard
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    ports:
      - "8080:80"